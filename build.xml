<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See commented blocks below for -->
<!-- some examples of how to customize the build. -->
<!-- (If you delete it and reopen the project it will be recreated.) -->
<!-- By default, only the Clean and Build commands use this build script. -->
<!-- Commands such as Run, Debug, and Test only use this build script if -->
<!-- the Compile on Save feature is turned off for the project. -->
<!-- You can turn off the Compile on Save (or Deploy on Save) setting -->
<!-- in the project's Project Properties dialog box.-->
<project name="BibliotecaIngeSoftware" default="default" basedir=".">
    <description>Builds, tests, and runs the project BibliotecaIngeSoftware.</description>
    <import file="nbproject/build-impl.xml"/>

    <!--
    Etapa 2 - Arreglo de compilacion de las pruebas de GUIBiblia.

    NetBeans lanza la compilacion de las pruebas filtrando por el patron
    "**/*Test.java", asi que la clase auxiliar test/GUIBiblia/VentanaTestUtils.java
    queda fuera del lote enviado a javac. Como el target -do-compile-test de
    build-impl.xml compila con el sourcepath vacio, javac tampoco puede
    encontrarla por su cuenta y las 9 clases de prueba de ventanas fallan con
    "cannot find symbol: variable VentanaTestUtils" (59 errores, uno por cada
    uso de la clase).

    Al fijar aqui javac.test.sourcepath apuntando a la carpeta test, javac si
    puede localizar y compilar la clase auxiliar de forma implicita. Esta
    propiedad se define antes de que build-impl.xml la ponga en vacio, y en Ant
    la primera asignacion es la que manda.
    -->
    <property name="javac.test.sourcepath" value="test"/>
    <!--

    There exist several targets which are by default empty and which can be
    used for execution of your tasks. These targets are usually executed
    before and after some main targets. They are:

      -pre-init:                 called before initialization of project properties
      -post-init:                called after initialization of project properties
      -pre-compile:              called before javac compilation
      -post-compile:             called after javac compilation
      -pre-compile-single:       called before javac compilation of single file
      -post-compile-single:      called after javac compilation of single file
      -pre-compile-test:         called before javac compilation of JUnit tests
      -post-compile-test:        called after javac compilation of JUnit tests
      -pre-compile-test-single:  called before javac compilation of single JUnit test
      -post-compile-test-single: called after javac compilation of single JUunit test
      -pre-jar:                  called before JAR building
      -post-jar:                 called after JAR building
      -post-clean:               called after cleaning build products

    (Targets beginning with '-' are not intended to be called on their own.)

    Example of inserting an obfuscator after compilation could look like this:

        <target name="-post-compile">
            <obfuscate>
                <fileset dir="${build.classes.dir}"/>
            </obfuscate>
        </target>

    For list of available properties check the imported
    nbproject/build-impl.xml file.


    Another way to customize the build is by overriding existing main targets.
    The targets of interest are:

      -init-macrodef-javac:     defines macro for javac compilation
      -init-macrodef-junit:     defines macro for junit execution
      -init-macrodef-debug:     defines macro for class debugging
      -init-macrodef-java:      defines macro for class execution
      -do-jar:                  JAR building
      run:                      execution of project
      -javadoc-build:           Javadoc generation
      test-report:              JUnit report generation

    An example of overriding the target for project execution could look like this:

        <target name="run" depends="BibliotecaIngeSoftware-impl.jar">
            <exec dir="bin" executable="launcher.exe">
                <arg file="${dist.jar}"/>
            </exec>
        </target>

    Notice that the overridden target depends on the jar target and not only on
    the compile target as the regular run target does. Again, for a list of available
    properties which you can use, check the target you are overriding in the
    nbproject/build-impl.xml file.

    -->
    <target name="analizar-complejidad" description="Calcula la complejidad ciclomática de forma directa">
        <exec executable="C:\pmd-bin-7.26.0\bin\pmd.bat" failonerror="true">
            <arg value="check"/>
            <arg value="-d"/>
            <arg value="${basedir}/src"/>

            <arg value="-R"/>
            <arg value="category/java/design.xml/CyclomaticComplexity"/>

            <arg value="-f"/>
            <arg value="html"/>
            <arg value="-r"/>
            <arg value="${basedir}/reporte-complejidad.html"/>
        </exec>
    </target>

    <target name="analizar-metricas-clase" description="Genera el reporte de WMC, CBO y Cohesión usando un ruleset personalizado">
        <exec executable="C:\pmd-bin-7.26.0\bin\pmd.bat" failonerror="true">
            <arg value="check"/>
            <arg value="-d"/>
            <arg value="${basedir}/src"/>

            <arg value="-R"/>
            <arg value="${basedir}/reglas-metricas.xml"/>

            <arg value="-f"/>
            <arg value="html"/>
            <arg value="-r"/>
            <arg value="${basedir}/reporte-metricas.html"/>
        </exec>
    </target>

    <!--
    Etapa 3 - Medicion de cobertura de codigo con JaCoCo.

    NetBeans 25 ya no ofrece un plugin de "Code Coverage" en su catalogo (los
    antiguos se basaban en EMMA, discontinuado). Este target corre la suite de
    JUnit completa con el agente de JaCoCo adjunto y genera un reporte HTML,
    replicando el mismo patron que ya usan los targets de PMD de arriba.

    Antes de ejecutarlo:
      1) Descarga la distribucion binaria de JaCoCo (jacoco-x.y.z.zip) desde
         https://www.jacoco.org/jacoco/ (seccion Releases) y extraela, por
         ejemplo, a C:\jacoco-0.8.12 (mismo criterio que la carpeta de PMD).
      2) Ajusta la ruta del atributo "path" del <classpath> de abajo si tu
         version o carpeta de destino es distinta.
      3) Asegurate de que MySQL este corriendo (localhost:3306, base
         "biblioteca") para que las pruebas de GUIBiblia tambien se ejecuten.

    Para correrlo: igual que "analizar-metricas-clase", desde la vista Files
    de NetBeans, clic derecho sobre build.xml -> Run Target -> Other Targets
    -> medir-cobertura (o desde una terminal, dentro de la carpeta del
    proyecto: ant medir-cobertura).
    -->
    <target name="medir-cobertura" depends="compile-test"
            xmlns:jacoco="antlib:org.jacoco.ant"
            description="Corre las pruebas con el agente de JaCoCo y genera el reporte HTML de cobertura">
        <taskdef uri="antlib:org.jacoco.ant" resource="org/jacoco/ant/antlib.xml">
            <classpath path="C:\jacoco-0.8.12\lib\jacocoant.jar"/>
        </taskdef>

        <delete file="${basedir}/jacoco.exec" quiet="true"/>
        <delete dir="${basedir}/coverage-report" quiet="true"/>

        <jacoco:coverage destfile="${basedir}/jacoco.exec">
            <junit fork="true" forkmode="once" showoutput="true" printsummary="true"
                   failureproperty="cobertura.tests.failed">
                <classpath path="${run.test.classpath}"/>
                <formatter type="brief" usefile="false"/>
                <batchtest>
                    <fileset dir="${build.test.classes.dir}" includes="**/*Test.class"/>
                </batchtest>
            </junit>
        </jacoco:coverage>

        <jacoco:report>
            <executiondata>
                <file file="${basedir}/jacoco.exec"/>
            </executiondata>
            <structure name="BibliotecaIngeSoftware">
                <classfiles>
                    <fileset dir="${build.classes.dir}"/>
                </classfiles>
                <sourcefiles encoding="UTF-8">
                    <fileset dir="${src.dir}"/>
                </sourcefiles>
            </structure>
            <html destdir="${basedir}/coverage-report"/>
        </jacoco:report>

        <echo message="Reporte de cobertura generado en ${basedir}/coverage-report/index.html"/>
    </target>
</project>
